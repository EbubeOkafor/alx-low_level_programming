#include<stdio.h>
/**
 * main is the entry point of the program
 *
 * Return by default returns o if there are no errors else it
 * returns non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
